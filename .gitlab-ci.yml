---
image: jdubz/ci-docker-ansible:19.03

services:
  - docker:19.03.1-dind

molecule:
  stage: test
  before_script:
    - source /root/.bashrc
    - pyenv local 2.7.18 3.6.11 3.7.7 3.8.3
    - pip3 install tox
    - tox --version
  script:
    - tox
